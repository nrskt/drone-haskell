pipeline:

  restore-cache:
    image: drillster/drone-volume-cache
    restore: true
    mount:
      - ./.stack
    volumes:
      - /tmp/cache:/cache
    ttl: 7

  build:
    image: nrskt/haskell-build
    commands:
      - pwd
      - env